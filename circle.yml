machine:
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX
    - echo "[Machine] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log

checkout:
  post:
    - echo "[Checkout] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log

dependencies:
  post:
    - echo "[Dependencies] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log

database:
  post:
    - echo "[Database] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log

compile:
  post:
    - echo "[Compile] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log

test:
  override:
    - echo "[Tests] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log

deployment:
  override:
    - echo "[Deploy] Container is $CIRCLE_NODE_INDEX of $CIRCLE_NODE_TOTAL containers" >> $CIRCLE_ARTIFACTS/container-$CIRCLE_NODE_INDEX/steps.log
