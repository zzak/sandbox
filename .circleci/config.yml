version: 2
jobs:
  build-and-test:
    macos:
      xcode: "9.1"

    steps:
      - checkout
      - run:
          name: Output installed rubies
          command: chruby

workflows:
  version: 2
  build-and-test:
    jobs:
      - build-and-test
