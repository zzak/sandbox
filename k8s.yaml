kind: Deployment
metadata:
  name: hello-deployment
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
      - name: hello
        image: asia.gcr.io/zzak-gci/hello:latest
        ports:
        - containerPort: 80
